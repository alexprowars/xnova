<div id="tabs" class="ui-tabs ui-widget ui-widget-content techtree">
	<div class="head">
		<ul class="ui-tabs-nav ui-widget-header">
			<? foreach ($parse AS $i => $list): ?>
				<? if (!isset($list['required_list'])): ?>
					<li><a href="#tabs-<?=$i ?>"><?=$list['tt_name'] ?></a></li>
				<? endif; ?>
			<? endforeach; ?>
		</ul>
	</div>
	<div id="tabs-0" class="ui-tabs-panel ui-widget-content container-fluid">
		<? foreach ($parse AS $i => $list): if ($i == 0) continue; ?>
			<? if (!isset($list['required_list'])): ?>
				</div><div id="tabs-<?=$i ?>" class="ui-tabs-panel ui-widget-content container-fluid" style="display: none">
			<? else: ?>
				<div class="row">
					<div class="col-sm-5 col-xs-6 title">
						<a href="?set=infos&gid=<?=$list['tt_info'] ?>"><?=$list['tt_name'] ?></a>

						<? if ($list['required_list'] != ''): ?>
							<div class="pull-right hidden-xs"><a href="?set=techtree&id=<?=$list['tt_info'] ?>">[i]</a></div>
						<? endif; ?>
					</div><div class="col-sm-7 col-xs-6">
						<?=$list['required_list'] ?>
					</div>
				</div>
			<? endif; ?>
		<? endforeach; ?>
	</div>
</div>

<script type="text/javascript">
$(function()
{
  	$( "#tabs" ).tabs();
	$("#tabs .row:even").addClass("odd");
});
</script>