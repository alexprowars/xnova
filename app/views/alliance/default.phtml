<table width=650>
	<tr>
		<td class=c colspan=2>Альянсы</td>
	</tr>
	<tr>
		<th><a href="?set=alliance&mode=make">Создать альянс</a></th>
		<th><a href="?set=alliance&mode=search">Поиск альянса</a></th>
	</tr>
</table>
<? if (count($parse['list']) > 0): ?>
<br>
<table width=650>
	<tr>
		<td class=c colspan=2>Ваши заявки</td>
	</tr>
	<? foreach ($parse['list'] AS $list): ?>
	<tr>
		<th width=70%><?=$list[2] ?> [<?=$list[1] ?>]</th>
		<th><?=datezone("d.m.Y H:i", $list[3]) ?></th>
	</tr>
	<tr>
		<th colspan=2>
			<form action="?set=alliance" method=POST><input type="hidden" name="r_id" value="<?=$list[0] ?>"><input type=submit name="bcancel" value="Убрать заявку"></form>
		</th>
	</tr>
	<? endforeach; ?>
</table>
<? endif; ?>
<? if (count($parse['allys'])): ?>
	<br>
	<table width="650">
		<tr>
			<td class="c" width="30">Место</td>
			<td class="c">Альянс</td>
			<td class="c">Игроки</td>
			<td class="c">Очки</td>
		</tr>
		<? $i = 0; foreach ($parse['allys'] AS $list): $i++; ?>
		<tr>
			<th><?=$i ?></th>
			<th><a href="?set=alliance&mode=ainfo&a=<?=$list['id'] ?>"><?=$list['ally_name'] ?> [<?=$list['ally_tag'] ?>]</a></th>
			<th><?=$list['ally_members'] ?></th>
			<th><?=$list['total_points'] ?></th>
		</tr>
		<? endforeach; ?>
	</table>
<? endif; ?>